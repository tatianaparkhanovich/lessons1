import { Section } from "../section/Section";
import "./Main.css";

export const Main = (props) => (
  <div className="Main">
    <Section className="Section" />
    <Section className="Section" />
    <Section className="Section" />
  </div>
);
